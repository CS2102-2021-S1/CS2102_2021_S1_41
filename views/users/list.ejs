<!DOCTYPE html>
<html>
<body>
<head>
    <title>Pet Care System (PCS) SG - <%=title%></title>
</head>
<div class="page-header"><h2><%=title%></h2></div>
<div class="page-data">
    <div class="data-table">
        <table class="center">
            <tr>
                <th width="50px">No</th>
                <th>Login Name</th>
                <th>Name</th>
                <th>Password</th>
            </tr>
            <% if(data.length){
            for(var i = 0;i < data.length;i++) { %>
            <tr>
                <td><%=(i+1)%></td>
                <td><%=data[i].login_name%></td>
                <td><%=data[i].uname%></td>
                <td><%=data[i].password_hash%></td>
            </tr>
            <% }
            }else{ %>
                <tr>
                    <td colspan="6">No user</td>
                </tr>
            <% } %>
        </table>
    </div>
</div>
</body>
</html>